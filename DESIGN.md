# 디자인 시스템: Palace 개랜타디

## 1. Visual Theme & Atmosphere

Palace 개랜타디의 화면은 "모바일 판타지 전투 로비"처럼 보여야 한다. 기준 화면은 `LobbyPage`와 `HomeTab`이다. 횃불이 비추는 성 안뜰, 따뜻한 갈색 표면, 금색 포인트, 픽셀 UI가 극적인 배경 위에 얹힌 느낌이 핵심이다.

분위기는 아늑하지만 긴장감이 있어야 한다. 밤의 성 안뜰에서 전투를 준비하는 느낌. 배경에는 짙은 하늘 그라데이션과 약한 환경 연출이 들어갈 수 있지만, UI 자체는 따뜻하고 묵직하며 손에 잡히는 감각이어야 한다. 패널은 유리판이나 네온 HUD가 아니라, 깎인 목재와 낡은 금속, 양피지 오버레이처럼 보여야 한다.

이 게임은 모바일 우선이다. 전체 인터페이스는 하나의 세로형 게임 기기 화면처럼 읽혀야 한다. 상단 상태 바, 몰입감 있는 중앙 장면, 하단 내비게이션, 그리고 한눈에 들어오는 메인 CTA. 장식 효과는 횃불 흔들림, 별 반짝임, 깃발 흔들림 정도로 작고 느리게 들어가야 한다. 게임 플레이나 터치 타겟보다 앞서면 안 된다.

**핵심 특징**
- 어두운 목재와 횃불빛을 기반으로 한 갈색-금색 판타지 팔레트
- 픽셀 아트 장면 배경 위에 겹쳐지는 UI 오버레이
- 짧고 또렷한 픽셀 타이포그래피
- 부드러운 SaaS 카드 그림자가 아니라, 하드한 테두리와 오프셋 그림자
- 중앙 장면과 하단 조작부가 분명한 세로형 모바일 셸
- 차갑고 미래적인 느낌이 아니라, 전투 전 안뜰의 따뜻한 긴장감

## 2. Color Palette & Roles

### 핵심 UI 색상
- **Night Soil** (`#1a1208`): 전체 앱 배경, 모바일 셸 바탕, 탭 바 배경, 외곽 프레임
- **Carved Wood** (`#2a2010`): 기본 패널 표면, 카드, 프로필 바, CTA 컨테이너
- **Old Timber** (`#4a3a20`): 기본 테두리, 구분선, 저강도 윤곽선
- **Torch Gold** (`#c8a04a`): 기본 액센트, 강조 라벨, 프리미엄 포인트
- **Coin Gold** (`#f0d060`): 가장 강한 CTA 포인트, 재화, 활성 상태, 보상 강조
- **Parchment** (`#f0e8d8`): 본문 텍스트, 높은 대비가 필요한 주요 텍스트
- **Dusty Brass** (`#a09070`): 보조 텍스트, 비활성 라벨, 설명 문구

### 상태 색상
- **Victory Green** (`#7ab648`): 승리, 성공, 긍정적 진행
- **Alert Red** (`#c03020`): 위험, 취소, 패배, 파괴적 액션
- **Signal Blue** (`#5bc8e8`): 정보성 상태. 꼭 필요할 때만 제한적으로 사용

### 배경 그라데이션
- 로비 배경은 픽셀 아트 위에 짙은 하늘 계열 그라데이션을 깔 수 있다.
  - `#0d1a2a -> #14233a -> #1a1208`
  - 하단 가독성을 위한 투명 -> 갈색-검정 오버레이
- 이런 그라데이션은 장면을 받쳐주는 역할이다. 브랜드의 주색을 바꾸면 안 된다.

### 사용 원칙
- UI 크롬은 갈색 표면과 금색 포인트가 지배해야 한다.
- 기본 활성 신호는 금색이다. 빨강과 초록은 상태 표현용 예외다.
- 보라, 핑크, 청록이 화면 정체성을 지배하면 안 된다. 차가운 색은 배경 아트 안에서만 제한적으로 허용된다.

## 3. Typography Rules

### 폰트 패밀리
- **기본 폰트**: `Galmuri11`
- **보조 / 장식용 픽셀 폰트**: `'Press Start 2P', cursive`

UI는 분명하게 픽셀 스타일이어야 하지만, 모바일에서 읽기 어려우면 안 된다. 실제 플레이 중 텍스트는 대부분 `Galmuri11`을 우선 사용한다. `Press Start 2P`는 더 강한 레트로 포인트가 필요한 경우에만 제한적으로 쓴다.

### 텍스트 계층

| 역할 | 폰트 | 크기 | 굵기 | 용도 |
|------|------|------|------|------|
| 히어로 CTA | Galmuri11 | 11px | 400 | 메인 버튼, 전투 카드 타이틀 |
| 기본 UI 라벨 | Galmuri11 | 10px | 400 | 버튼, 스탯, 패널 제목 |
| 일반 UI | Galmuri11 | 9px | 400 | 프로필명, 주요 라벨 |
| 보조 UI | Galmuri11 | 8px | 400 | 재화, 메타데이터, 오버레이 컨트롤 |
| 서브 UI | Galmuri11 | 7px | 400 | 탭 라벨, 설명 문구, 작은 상태 표시 |
| 마이크로 UI | Galmuri11 | 6px | 400 | 배지, 아주 작은 라벨, 숫자 카운트 |

### 타이포그래피 원칙
- 버튼과 탭 텍스트는 짧게 간다. 한국어 기준 1~3단어가 적절하다.
- 스탯은 숫자 위, 라벨 아래 패턴을 적극 사용한다.
- 설명 문구는 투명도를 과하게 낮추기보다 보조 텍스트 색으로 해결한다.
- 긴 대문자 시스템 라벨을 남발하지 않는다. 게임 UI처럼 보여야지 운영 대시보드처럼 보이면 안 된다.
- 얇은 현대식 산세리프를 메인 UI에 섞지 않는다.

### UX Writing 원칙
- **모든 사용자 대면 텍스트는 한글로 작성한다.** 영어 표기 금지. 시스템 내부 키(코드)는 영어여도 되지만, 화면에 보이는 라벨, 설명, 토스트, 버튼 텍스트는 반드시 한글이어야 한다.
- 게임 용어는 판타지 세계관에 맞게 번역한다. (예: fire→화염, water→냉기, lightning→번개, neutral→무속성, splash→범위 공격, stun→기절, slow→감속)
- 영어 약어(HP, XP, G 등)는 게임 관습상 허용한다.
- 존댓말 통일. 토스트/안내 문구는 "~합니다" 체.
- 짧고 직접적으로. 한 문장에 정보 하나.

## 4. Component Stylings

### 모바일 셸
- 최대 너비: `430px`
- 전체 높이를 쓰는 세로형 구조
- 바깥은 어두운 여백, 안쪽은 하나의 기기 화면처럼 강하게 묶인다
- 웹페이지 섹션들의 느슨한 나열처럼 보이면 안 된다

### 상단 프로필 바
- 표면: `rgba(42, 32, 16, 0.85)`
- 하단 보더: `1px solid #4a3a20`
- 왼쪽 아바타, 중앙 닉네임/레벨, 오른쪽 재화 묶음 구조
- 아이콘은 작고 선명한 픽셀 렌더링을 유지한다

### 하단 탭 바
- 표면: `rgba(26, 18, 8, 0.95)`
- 상단 경계선만 두고 크롬은 최소화
- 아이콘 우선, 텍스트는 보조
- 활성 상태는 금색 계열 텍스트나 아이콘으로 표현한다
- 과한 글로우나 네온 그라데이션은 쓰지 않는다
- 비활성 상태는 Dusty Brass 계열로 내려간다

### 메인 CTA 카드
- 높은 불투명도의 갈색 패널
- 보더: `2px solid #f0d060`
- 금색 테두리, 오프셋 그림자, 약한 따뜻한 글로우를 함께 사용
- 화면 하단 3분의 1에서 가장 먼저 보이는 대상이어야 한다
- 보조 버튼은 메인 버튼보다 분명히 한 단계 아래여야 한다

### 버튼
- 기본 버튼 구조
  - 배경: `#2a2010`
  - 보더: variant 색상의 `2px solid`
  - 그림자: 같은 색 `4px 4px 0px`
  - 폰트: 픽셀 폰트, 보통 10px
- Hover / Press
  - 그림자 오프셋이 줄고 위치가 약간 이동하는 식으로 눌림 감각을 준다
  - 유리 반사, 부드러운 팝업 스케일, 젤리 애니메이션은 금지
- 버튼 변형
  - **Gold**: 가장 중요한 액션, 전투 시작, 보상 확인
  - **Primary**: 기본 액션
  - **Secondary**: 우선순위 낮은 보조 액션
  - **Danger**: 취소, 이탈, 파괴적 액션

### 패널
- 부드러운 카드보다, 단단한 테두리와 깊이감이 우선
- 기본 패턴
  - 배경: `#2a2010`
  - 보더: `2px solid #4a3a20`
  - 그림자: `4px 4px 0px #4a3a20`
  - 패딩: `16px`
- 패널은 공중에 뜬 반투명 카드가 아니라, 조각된 UI 블록처럼 보여야 한다

### 오버레이 아이콘과 배지
- 작은 정사각형 유틸리티 타일 허용
- 빨간 숫자 배지로 카운트를 얹는다
- 라벨은 짧고 직설적으로 간다
- 아이콘 에셋이 없을 때도 텍스트 fallback이 브라우저 기본 UI처럼 보이면 안 된다

### 배경 아트 레이어링
- 픽셀 아트 배경은 `object-fit: cover`로 화면을 채운다
- 하단 UI 가독성을 위해 아래쪽을 어둡게 누르는 오버레이를 추가한다
- 횃불, 별, 깃발 같은 환경 애니메이션은 작고 느려야 한다

### 매치메이킹 / 풀스크린 오버레이
- `rgba(10, 8, 4, 0.88)` 수준의 갈흑색 스크림 사용
- 중앙에 하나의 심볼이나 애니메이션을 둔다
- 레이아웃은 비워둔다. 복잡하면 안 된다
- 메인 텍스트는 금색, 취소 액션은 위험 스타일 사용

## 5. Layout Principles

### 간격 시스템
- 기본 간격은 촘촘하다. 주로 `2px`, `3px`, `4px`, `6px`, `8px`, `10px`, `12px`, `14px`, `16px`, `20px`
- 픽셀 UI + 모바일 전제를 유지하기 위해 과하게 넓히지 않는다
- 카드 내부는 작은 세로 간격으로 리듬을 만든다. 셸이 부풀어 오르면 안 된다

### 화면 구성
- 상단은 신분/경제 상태
- 중단은 장면 또는 플레이 영역
- 하단은 액션 밀집 구간
- 화면마다 가장 강한 CTA는 하나만 둔다

### 여백 철학
- 이 제품은 조밀하지만 답답하면 안 된다
- 넓은 마케팅 페이지식 공백보다, 압축된 게임 UI 리듬을 따른다
- 메인 CTA 카드 주변과 모달 오버레이 주변에는 숨 쉴 공간을 둔다
- 배경의 빈 공간은 액션을 돋보이게 할 때만 가치가 있다

### 정보 밀도
- 로비는 더 분위기 있고 레이어드되어도 된다
- 게임 HUD는 더 실용적이고 절제되어야 한다
- 애매하면 장식보다 가독성을 우선한다. 특히 전투 중에는 더 그렇다

## 6. Depth & Elevation

| 레벨 | 처리 방식 | 용도 |
|------|-----------|------|
| Level 0 | 평평한 배경 또는 아트만 사용 | 장면 배경, 빈 캔버스 |
| Level 1 | `1px solid #4a3a20` | 구분선, 저강도 바 |
| Level 2 | `2px` 보더 + `4px 4px 0px` 오프셋 그림자 | 기본 패널, 버튼 |
| Level 3 | Level 2 + 약한 따뜻한 글로우 | 메인 CTA 카드, 강조 모듈 |
| Level 4 | 전체 스크림 + 중앙 집중 패널 | 매치메이킹, 모달 포커스 상태 |

### 깊이감 원칙
- 부드러운 블러 그림자보다 하드한 오프셋 그림자를 우선한다.
- 글로우는 양념이다. 깊이의 주재료가 되면 안 된다.
- 패널은 판타지 보드게임 상자 안의 물리적 UI 조각처럼 보여야 한다.
- 프로스트 글라스, 반투명 흰 안개, 테크 제품식 블러는 금지한다.

## 7. Do's and Don'ts

### Do
- 새 UI를 만들 때는 항상 로비 감각에서 출발한다. `HomeTab` 옆에 두어도 어색하지 않으면 거의 맞다.
- 따뜻한 갈색, 오래된 금색, 양피지 텍스트, 픽셀 아이콘을 사용한다.
- 보더와 오프셋 깊이감으로 장난감 같은 촉감을 만든다.
- 드라마는 배경 일러스트가 담당하고, 전경 UI는 읽기 쉽게 유지한다.
- 작은 환경 애니메이션으로 세계가 살아 있다고 느끼게 한다.

### Don't
- 사이버펑크 보라, 네온 핑크, e스포츠 청록, 차가운 미래형 HUD를 정체성으로 쓰지 않는다.
- 글래스모피즘, 블러 카드, 제너릭 SaaS 대시보드 컴포넌트를 쓰지 않는다.
- 얇은 현대식 타이포, 과한 둥근 pill, 앱스토어식 광택 UI를 들이지 않는다.
- 중요한 게임 플레이 영역을 UI로 불필요하게 가리지 않는다.
- 모든 블록을 카드처럼 만들지 않는다. 이건 설정 웹사이트가 아니라 게임 셸이다.

## 8. Responsive Behavior

### 기본 원칙
- 좁은 세로 화면 우선
- 넓은 화면에서는 셸이 중앙에 유지된다
- 텍스트가 줄어들어도 터치 타겟은 유지한다

### 작은 화면
- 너무 좁아지면 모든 요소를 축소하기보다 우선순위 낮은 정보를 먼저 숨긴다
- 현재 허용되는 축소 방식
  - 재화 정보 일부 숨김
  - 탭 라벨이 아이콘보다 먼저 축소 또는 제거
- 메인 액션은 어떤 폭에서도 읽히고 눌려야 한다

### Safe Area
- 하단 내비게이션과 고정 컨트롤은 safe area inset을 반영한다
- 중요한 탭 타겟을 화면 끝에 딱 붙이지 않는다

### 모션
- `prefers-reduced-motion`을 지원한다
- 환경 애니메이션은 깔끔하게 꺼질 수 있어야 한다
- 기능 전환은 남아도 되지만, 빠른 움직임에 이해를 의존하면 안 된다

## 9. Agent Prompt Guide

### 빠른 시각 기준
- 따뜻한 중세 픽셀 판타지 모바일 게임 로비처럼 만든다
- 기준은 로비다. 특히 `LobbyPage`, `ProfileBar`, `BottomTabBar`, `HomeTab`
- 조각된 갈색 패널, 금색 트림, 양피지 텍스트, 픽셀 아트 배경을 우선한다
- 전체 화면이 하나의 세로형 핸드헬드 게임 클라이언트처럼 느껴져야 한다

### 바로 쓸 수 있는 프롬프트
- "Palace 로비 느낌을 최우선 기준으로 사용해. 따뜻한 횃불빛 갈색, 금색 포인트, 픽셀 아트 분위기, 컴팩트한 모바일 UI."
- "이 화면이 기존 Home 탭 옆에 있어도 어색하지 않게 만들어. 성 안뜰 판타지 분위기, 또렷한 픽셀 타이포, 촉감 있는 보더 패널."
- "SF HUD처럼 만들지 말고, 성에서 전투를 준비하는 로비처럼 만들어."
- "장식은 좋지만 게임 가독성이 우선이다. 분위기는 살리고 UI 크롬은 실용적으로 유지해."

### 앞으로의 정렬 원칙
- 새 화면이 로비 감각과 충돌하면 로비 쪽이 정답이다.
- 게임 HUD가 복잡하면, 팔레트를 바꾸기 전에 먼저 레이아웃을 단순화한다.
- 기존 게임 UI 일부에 남아 있는 차가운 색감이나 네온 흔적은 legacy 스타일로 취급한다. 장기 기준이 아니다.
