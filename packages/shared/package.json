{
  "name": "@gld/shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
	"scripts": {
		"test": "vitest run",
		"test:watch": "vitest",
		"lint": "biome check --write .",
		"build:unity-json": "bun run ../../scripts/export-shared-to-json.ts",
		"replay:record": "bun run src/testing/replay-runner.ts ../../artifacts/phase-3-replay-metrics.csv",
		"replay:soak": "bun run replay:record"
	},
  "devDependencies": {
    "vitest": "^3.0.0",
    "typescript": "^5.7.0"
  }
}
