{
  "buildCommand": "tsc -b packages/web-shell && cd packages/web-shell && bunx vite build",
  "outputDirectory": "packages/web-shell/dist",
  "installCommand": "bun install",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
